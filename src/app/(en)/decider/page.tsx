import DeciderLanding, { deciderMetadata } from "@/components/DeciderLanding";

export const metadata = deciderMetadata;

export default function DeciderPage() {
  return <DeciderLanding locale="en" />;
}
